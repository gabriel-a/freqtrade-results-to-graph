jest.setTimeout(30000); // 30 seconds

// // Ensure Puppeteer closes the browser after all tests
// afterAll(async () => {
//     if (global.browser) {
//         await global.browser.close();
//     }
// });
